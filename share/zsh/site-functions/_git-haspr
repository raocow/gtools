#autoload
#
# Completion for `git haspr [branch]` — real existing branches.
if ! git rev-parse --is-inside-work-tree >/dev/null 2>&1; then
  _message -r 'not inside a git repository'; return
fi
_alternative \
  'flags:Flag:(-h --help)' \
  'branches:Branch:_gitplus_branches'
